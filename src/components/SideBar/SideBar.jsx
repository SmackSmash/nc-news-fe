import Button from '../Button/Button';
import './SideBar.css';

const SideBar = () => {
  return (
    <aside id='sideBar'>
      <Button>+ Add Topic</Button>
    </aside>
  );
};

export default SideBar;
